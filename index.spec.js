const expect = require('chai').expect;
const printBoard = require('./index').printBoard;

describe('Print board function', function() {
  it('should add properly', function() {
    expect(1 + 1).to.equal(3);
  })
})